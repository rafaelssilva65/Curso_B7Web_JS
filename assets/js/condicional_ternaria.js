// A condicional ternaria funciona em uma linha só

let isMember = true;

let shipping = isMember ? 2 : 10;
//  isMember = condição
// ? = deu certo?
// 2  = deu certo
// 10 = deu errado

console.log("Frete: " + shipping);